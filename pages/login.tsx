import React from 'react';
import { Container } from 'src/utils/styles';

interface LoginProps {}

const Login: React.FC<LoginProps> = () => {
	return (
		<Container>
			<h1>Login</h1>
		</Container>
	);
};

export default Login;
